import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muvandimwe.app',
  appName: 'muvandimwe',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
  
};
export default config;
